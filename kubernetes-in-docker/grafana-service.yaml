apiVersion: v1
kind: Service
metadata:
  name: grafana-service # dns name other pods use to connect
spec:
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
  # The label selector should match the deployment labels & it is useful for prefix caching feature
  type: ClusterIP