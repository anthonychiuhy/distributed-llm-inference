apiVersion: v1
kind: Service
metadata:
  name: traffic-generator-service # dns name other pods use to connect
spec:
  selector:
    app: traffic-generator
  ports:
  - port: 8080
    targetPort: 8080
  # The label selector should match the deployment labels & it is useful for prefix caching feature
  type: ClusterIP